## 1. TDD 契約與 fixtures

- [x] 1.1 依「Plugin inventory invokes only fixed read-only CLI capabilities」、「CLI execution is bounded and failures are sanitized」與 design「Fixed Plugin CLI capability table」、「Bounded asynchronous process runner and sanitized failures」，在 `src-tauri/src/core/plugin_inventory.rs` 先建立 failing tests，固定六組 argv、no-shell／closed-stdin seam、10-second timeout、child reap、1,048,576／1,048,577-byte stdout／stderr boundaries及fixed diagnostics；逐支執行 `cargo test --manifest-path src-tauri/Cargo.toml --locked plugin_inventory::tests::runner_ -- --nocapture`，確認實作前至少一支因 module 或 API 尚不存在而 FAILED。
- [x] 1.2 依「Agent-specific JSON is normalized without invented values」、「Inventory identity, merge, and ordering are deterministic」與 design「Agent-specific JSON parsers and lossless unknown states」，建立 Codex 0.144.5／Claude Code 2.1.229 installed＋available＋marketplace JSON fixtures，涵蓋missing／unknown fields、opaque version、additive fields、same id across Agent／marketplace、same-key merge與shuffled arrays；執行 `plugin_inventory::tests::parse_*` 及 `plugin_inventory::tests::normalize_*`，固定unknown不被推測且兩次結果排序相同。
- [x] 1.3 依「Agent and marketplace failures are isolated」、「Plugin inventory is transient and excludes sensitive payload」與 design「Agent-isolated aggregation without persistence」，建立missing CLI、unsupported CLI contract、marketplace offline、non-zero exit、invalid JSON及含 `sentinel-secret` output fixtures；執行 `plugin_inventory::tests::aggregate_*`，固定單一 capability 失敗不隱藏其他結果，且serialized DTO、logs、SQLite dump、Library tree與Git status都不含payload或新row。
- [x] 1.4 依「Plugins page exposes complete read-only inventory」與 design「Read-only Plugins route and route-local state」，先建立 `scripts/check-plugins-ui.mjs` 的 failing static contract，固定 `/plugins` route、Sidebar link、五組filters、unknown顯示、diagnostics、latest-request-wins、route-local state、locale parity及mutation／validation／details／eval controls全數缺席；執行 `node scripts/check-plugins-ui.mjs` 確認頁面實作前 FAILED。

## 2. Backend runner、normalization 與 IPC

- [x] 2.1 依 design「Fixed Plugin CLI capability table」與「Bounded asynchronous process runner and sanitized failures」，在 `src-tauri/src/core/plugin_inventory.rs` 使用既有 Tokio 實作enum-backed command table、closed stdin、concurrent bounded stream readers、10-second timeout、kill／reap及fixed diagnostic DTO，使production API無caller-controlled executable／args且raw output不進diagnostic／log；以1.1全部runner tests及 `cargo test --manifest-path src-tauri/Cargo.toml --locked plugin_inventory::tests::runner_ -- --nocapture` exit 0驗證。
- [x] 2.2 依 design「Agent-specific JSON parsers and lossless unknown states」，實作分離的 Codex／Claude Code parsers、closed unknown enums、opaque versions、Agent＋marketplace＋plugin id identity、same-key known-value merge與deterministic sort，使additive fields不破壞supported response且缺值保持unknown；以1.2 parse／normalize fixtures逐欄比較並要求相關tests exit 0。
- [x] 2.3 依 design「Agent-isolated aggregation without persistence」，實作每個Agent與capability獨立收集的 `PluginInventoryDto`，在 `src-tauri/src/commands/plugins.rs`、`src-tauri/src/commands/mod.rs`、`src-tauri/src/core/mod.rs`與`src-tauri/src/lib.rs`註冊無request fields的 `get_plugin_inventory`，使一側失敗仍回傳另一側且production path無`SkillStore`／Library／cache writes；以1.3 aggregation tests及command serialization test驗證fixed response／error shape與zero persistence side effects。

## 3. Frontend read-only Plugins 頁面

- [x] 3.1 在 `src/lib/tauri.ts` 建立與backend closed enums一致的 Plugin inventory型別及無參數 `getPluginInventory` wrapper，使frontend無法提交executable／path／args且unknown狀態不轉為boolean；以TypeScript build與static contract的IPC assertions驗證request shape。
- [x] 3.2 依 design「Read-only Plugins route and route-local state」，在 `src/views/Plugins.tsx` 實作Agent availability、inventory rows、read-only details、diagnostics、Agent／presence／scope／marketplace／status filters與latest-request-wins refresh，使unknown可見、diagnostics不受filters隱藏且資料不進`AppContext`／localStorage；以1.4 UI contract及manual rapid refresh驗證舊response不覆蓋新state。
- [x] 3.3 在 `src/App.tsx`、`src/components/Sidebar.tsx`、`src/i18n/en.json`、`src/i18n/zh-TW.json`、`package.json`與 `scripts/check-plugins-ui.mjs` 完成functional `/plugins` route、Sidebar link、雙語文案與 `check:plugins-ui` script，使install／update／remove／enable／disable／marketplace mutation／validate／details／eval controls保持缺席；執行 `npm run check:plugins-ui`與`npm run check:i18n`要求exit 0。

## 4. 完整驗證與安全證據

- [x] 4.1 執行 `cargo test --manifest-path src-tauri/Cargo.toml --locked`、`npm run build`、`npm run lint`、`npm run check:i18n`、`npm run check:plugins-ui`與`git diff --check`，要求所有commands exit 0；再以temporary fake Codex／Claude executables完成success、missing CLI、timeout、oversized output、non-zero exit、invalid JSON與marketplace offline流程，逐值比較argv、normalized DTO、SQLite dump、Library tree、官方cache fixtures、Git status、logs與localStorage，確認只有當次bounded process memory與IPC中的normalized non-sensitive fields可含inventory資料，且沒有任何Plugin mutation被呼叫。
