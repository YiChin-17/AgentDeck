## 1. TDD 契約與 fixtures

- [x] 1.1 依「Plugin mutations use an Agent-specific fixed capability matrix」、「Mutation selectors are derived from fresh inventory」與 design「Agent-specific fixed mutation capability matrix」，在 `src-tauri/src/core/plugin_mutation.rs` 先建立 failing table tests，固定 Codex install／remove、Claude install／update／remove／enable／disable、Codex unknown inventory scope不阻擋fixed user operation、Claude user scope、`--` selector terminator、512-byte identity boundary及所有 forbidden args；分別執行 `cargo test --manifest-path src-tauri/Cargo.toml --locked plugin_mutation::tests::matrix_ -- --nocapture` 與 `cargo test --manifest-path src-tauri/Cargo.toml --locked plugin_mutation::tests::selector_ -- --nocapture`，確認實作前至少一支因 module 或 API 尚不存在而 FAILED。
- [x] 1.2 依「Mutation preview binds one intent to a transient token」與 design「Fresh-inventory preview and one-time token」，建立 fresh inventory fixtures 與可控 clock／UUID seam，涵蓋exact identity、operation precondition、SHA-256 canonical fingerprint、120-second expiry、128-entry eviction及serialized response；逐支執行 `plugin_mutation::tests::preview_*`，確認 preview 實作前 FAILED 且 fixtures 不含 path、credential 或 Plugin payload。
- [x] 1.3 依「Apply consumes the token and rejects stale state」與 design「Single-use apply with stale and concurrency gates」，建立 fake collector／runner 與 concurrency fixtures，固定missing、expired、replayed token、CLI version／capability／presence／version／enabled drift及兩個 Agent 同時 apply；逐支執行 `plugin_mutation::tests::apply_*`，確認 stale／replay 不呼叫 runner且同時 mutation 最大值為 1。
- [x] 1.4 依「Mutation execution remains bounded and sanitized」與 design「Bounded mutation runner and interactive-confirmation classification」，建立 no-shell、closed-stdin、10-second timeout、kill／reap、stdout／stderr 1,048,576／1,048,577-byte及Claude non-TTY phrase fixtures；逐支執行 `plugin_mutation::tests::runner_*`，確認 exact bounds、typed codes、numeric exit status及serialized diagnostic不含 `sentinel-secret`。
- [x] 1.5 依「Mutation success requires post-operation inventory proof」與 design「Post-mutation inventory verification」，建立 install／remove／enable／disable／update before-after tables與refresh failure fixtures；執行 `plugin_mutation::tests::verify_*`，固定exit 0但postcondition不成立、state unknown或refresh failure都回 `verification_failed`，且只有符合表格的fresh inventory回success。
- [x] 1.6 依「Plugins page previews and confirms only supported mutations」與 design「Capability-gated UI and destructive confirmation」，先建立 `scripts/check-plugin-mutations.mjs` 的 failing static contract，固定backend matrix、preview-first、token-only apply、unsupported／non-user disabled reasons、remove destructive fields、cancel no-op、fresh inventory replacement、failure refresh、route-local token及locale parity；執行 `node scripts/check-plugin-mutations.mjs` 確認 UI mutation 實作前 FAILED。

## 2. Backend mutation safety boundary

- [x] 2.1 依 design「Agent-specific fixed mutation capability matrix」，在 `src-tauri/src/core/plugin_mutation.rs` 實作enum-backed Agent／operation table與fresh-inventory selector validation，使production API只能產生規格固定的user-scope argv且unsupported operation在spawn前失敗；以1.1全部matrix／selector tests及 `cargo test --manifest-path src-tauri/Cargo.toml --locked plugin_mutation::tests::matrix_ -- --nocapture` exit 0驗證。
- [x] 2.2 依 design「Fresh-inventory preview and one-time token」，實作`PluginMutationState`、canonical fingerprint、UUID v4 token、120-second TTL與128-entry deterministic eviction，使`preview_plugin_mutation`只對fresh exact identity及有效precondition建立memory-only intent；以1.2全部`preview_*` tests逐欄比較response並要求exit 0。
- [x] 2.3 依 design「Single-use apply with stale and concurrency gates」，實作token原子消耗、global async mutation gate與apply前inventory fingerprint重驗，使missing／expired／replayed／stale request不spawn且跨Agent mutation序列化；以1.3全部`apply_*` tests及runner invocation count／max concurrency assertions驗證。
- [x] 2.4 依 design「Bounded mutation runner and interactive-confirmation classification」，沿用既有Tokio process primitives實作closed stdin、concurrent bounded streams、deadline、kill／reap及fixture-pinned ephemeral stderr classification，使raw output不進DTO或log且不傳`-y`；以1.4全部`runner_*` tests要求exit 0並逐值核對fixed diagnostics。
- [x] 2.5 依 design「Post-mutation inventory verification」，在CLI exit 0後重新收集inventory並實作五種operation postconditions，使unknown、unchanged或refresh failure回含latest sanitized inventory的`verification_failed`而不宣告成功；以1.5全部`verify_*` table tests及success serialization test要求exit 0。

## 3. IPC 與 Plugins 頁面

- [x] 3.1 在 `src-tauri/src/commands/plugin_mutation.rs`、`src-tauri/src/commands/mod.rs`、`src-tauri/src/core/mod.rs`、`src-tauri/src/lib.rs`與`src/lib/tauri.ts` 註冊managed memory state、`preview_plugin_mutation`及token-only `apply_plugin_mutation` DTO，使frontend無法提交scope／executable／cwd／environment／argv且command不接收Store或Library path；以Rust command serialization tests、TypeScript build及1.6 IPC static assertions驗證request／response shape。
- [x] 3.2 依「Plugins page exposes complete read-only inventory」與 design「Capability-gated UI and destructive confirmation」，在 `src/views/Plugins.tsx` 保留route-local唯讀inventory並依backend matrix與item state顯示操作，Claude non-user scope保持disabled（install接受available-only unknown scope）而Codex exact record使用fixed user capability，所有click先preview，remove使用同一preview顯示Agent／Plugin／marketplace／scope／argv並只送token apply，使unsupported controls不呼叫IPC、cancel no-op、verified inventory取代route-local data且failure refresh；以1.6 UI contract與manual fake-IPC success／stale／cancel流程驗證。
- [x] 3.3 在 `src/i18n/en.json`、`src/i18n/zh-TW.json`、`package.json`與`scripts/check-plugin-mutations.mjs` 完成fixed operations、disabled reasons、confirmation及diagnostic雙語文案與`check:plugin-mutations` script，使兩份locale key完全一致且頁面不出現`-y`、arbitrary config、marketplace mutation、validation、details、eval或prune controls；執行 `npm run check:plugin-mutations`與`npm run check:i18n`要求exit 0。

## 4. 完整驗證與安全證據

- [x] 4.1 執行 `cargo test --manifest-path src-tauri/Cargo.toml --locked`、`npm run build`、`npm run lint`、`npm run check:i18n`、`npm run check:plugins-ui`、`npm run check:plugin-mutations`與`git diff --check`，要求所有commands exit 0；再以temporary fake Codex／Claude executables逐值驗證supported argv、unsupported pre-spawn failure、expired／replayed／stale tokens、interactive confirmation、timeout、oversized output、non-zero exit、五種postconditions與serialized payload，並比較SQLite dump、Library tree、官方cache fixture、Git status、logs與localStorage，確認只有official fake CLI的預期state發生變更且AgentDeck沒有持久化token、stream或Plugin payload。
